{
  "rules": {
    "users": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth != null && auth.uid === $uid"
      }
    },

    "publicMessages": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}
