{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid"
      }
    },

    "publicMessages": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}
